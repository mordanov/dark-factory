apiVersion: apps/v1
kind: Deployment
metadata:
  name: uim-frontend
  namespace: dark-factory
spec:
  replicas: 1
  selector:
    matchLabels:
      app: uim-frontend
  template:
    metadata:
      labels:
        app: uim-frontend
    spec:
      imagePullSecrets:
        - name: ghcr-pull-secret
      containers:
        - name: uim-frontend
          image: ghcr.io/mordanov/uim-frontend
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 10
