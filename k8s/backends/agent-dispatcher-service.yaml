apiVersion: v1
kind: Service
metadata:
  name: agent-dispatcher
  namespace: dark-factory
  labels:
    app: agent-dispatcher
spec:
  selector:
    app: agent-dispatcher
  ports:
    - name: http
      port: 8000
      targetPort: 8000
