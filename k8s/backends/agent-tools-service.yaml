apiVersion: v1
kind: Service
metadata:
  name: agent-tools
  namespace: dark-factory
  labels:
    app: agent-tools
spec:
  selector:
    app: agent-tools
  ports:
    - name: http
      port: 8000
      targetPort: 8000
