apiVersion: v1
kind: Service
metadata:
  name: context-distiller
  namespace: dark-factory
  labels:
    app: context-distiller
spec:
  selector:
    app: context-distiller
  ports:
    - name: http
      port: 8000
      targetPort: 8000
