apiVersion: v1
kind: Service
metadata:
  name: user-input-manager
  namespace: dark-factory
  labels:
    app: user-input-manager
spec:
  selector:
    app: user-input-manager
  ports:
    - name: http
      port: 8000
      targetPort: 8000
