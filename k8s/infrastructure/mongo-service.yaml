apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: dark-factory
spec:
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None  # Headless for StatefulSet DNS stability
